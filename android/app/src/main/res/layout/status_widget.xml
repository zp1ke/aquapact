<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    style="@style/Widget.Android.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.Android.AppWidgetContainer"
    android:gravity="center">

    <FrameLayout
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_centerInParent="true">

        <!-- Gradient background -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/widget_background_gradient"
            android:scaleType="fitXY" />

        <!-- Background water drop (gray/outline) -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/water_drop_logo"
            android:alpha="0.3"
            android:scaleType="fitCenter" />

        <!-- Progress bar that clips the water drop from bottom to top -->
        <ProgressBar
            android:id="@+id/water_drop_progress"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:indeterminate="false"
            android:progress="0"
            android:max="10000"
            android:progressDrawable="@drawable/water_drop_fill_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:scaleType="fitCenter" />

        <!-- Percentage text in center -->
        <TextView
            android:id="@+id/percentage_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="0%"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:shadowColor="#000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4" />

    </FrameLayout>

    <!-- Motivational text below the water drop -->
    <TextView
        android:id="@+id/motivation_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_root"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="8dp"
        android:text="Start your day! 💧"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text_color"
        android:background="@drawable/text_background"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:gravity="center" />

</RelativeLayout>