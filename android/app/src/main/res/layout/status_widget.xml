<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    style="@style/Widget.Android.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.Android.AppWidgetContainer"
    android:padding="8dp">

    <!-- Motivational text at the bottom -->
    <TextView
        android:id="@+id/motivation_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="8dp"
        android:text="Start your day! 💧"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text_color"
        android:background="@drawable/text_background"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:gravity="center" />

    <!-- Water drop container takes remaining space above text -->
    <FrameLayout
        android:id="@+id/water_drop_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/motivation_text"
        android:layout_marginBottom="4dp"
        android:gravity="center">

        <!-- Gradient background -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/widget_background_gradient"
            android:scaleType="fitCenter" />

        <!-- Simple fill progress from bottom (behind water drop) -->
        <ProgressBar
            android:id="@+id/water_drop_progress"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:indeterminate="false"
            android:progress="0"
            android:max="10000"
            android:progressDrawable="@drawable/simple_fill_progress"
            style="?android:attr/progressBarStyleHorizontal" />

        <!-- Water drop image on top (shows through with transparency) -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/water_drop_logo"
            android:alpha="0.55"
            android:scaleType="fitCenter" />

        <!-- Percentage text in center -->
        <TextView
            android:id="@+id/percentage_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="0%"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:shadowColor="#000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4" />

    </FrameLayout>

</RelativeLayout>